node_modules/
dist/
.expo/
/ios/
/android/
modules/*/ios/build/
modules/*/android/build/
*.jks
*.p8
*.p12
*.key
*.mobileprovision
credentials.json
credentials/
*.orig.*
web-build/
build/
.tmp/

# Keep only the selected production character loops in Git. The same folder
# also contains local generation experiments that must not ship with the app.
assets/catalog/character-video-samples/*
!assets/catalog/character-video-samples/*-grok-eyebrow-hair-only-batch5.json
!assets/catalog/character-video-samples/*-grok-eyebrow-hair-only-batch5-boomerang.mp4
