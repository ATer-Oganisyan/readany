/**
 * Единая точка входа для transient-уведомлений мобильного приложения.
 *
 * Компонент Toaster монтируется один раз в App.tsx, поэтому toast можно
 * безопасно вызывать из экранов, хуков и сервисов — в том числе вне React.
 */
export { toast } from "sonner-native";
