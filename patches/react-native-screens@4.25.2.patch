diff --git a/ios/RNSScreenStack.mm b/ios/RNSScreenStack.mm
index bfd0a519a09eb346dfd687fe2feb0b5489d34c32..57b25baa105ca586e870d5e060d4977fdcc7c7be 100644
--- a/ios/RNSScreenStack.mm
+++ b/ios/RNSScreenStack.mm
@@ -494,6 +494,13 @@ - (void)updateContainer
         return;
       }
-
+
+      if ([next isKindOfClass:[RNSScreen class]] &&
+          ((RNSScreen *)next).screenView.stackPresentation == RNSScreenStackPresentationFormSheet) {
+        [[NSNotificationCenter defaultCenter]
+            postNotificationName:@"ReadAnyRNSScreenWillPresentFormSheet"
+                          object:next];
+      }
+
       [previous presentViewController:next
                              animated:shouldAnimate
                            completion:^{
@@ -1099,7 +1099,7 @@ - (BOOL)isScrollViewPanGestureRecognizer:(UIGestureRecognizer *)gestureRecognize
 // https://github.com/facebook/react-native/blob/69b131c2b1627f64f34a534dac4cf48a542e6ea6/packages/react-native/React/Views/ScrollView/RCTScrollView.m#L557
 - (BOOL)scrollViewHasHorizontalPart:(UIScrollView *)scrollView
 {
-  return scrollView.contentSize.width > scrollView.frame.size.width;
+  return scrollView.scrollEnabled && scrollView.contentSize.width > scrollView.frame.size.width;
 }
 
 // Custom method for compatibility with iOS < 13.4
@@ -1203,6 +1203,12 @@ - (BOOL)gestureRecognizer:(UIGestureRecognizer *)gestureRecognizer
   if (@available(iOS 26, *)) {
     if (gestureRecognizer == _controller.interactiveContentPopGestureRecognizer &&
         [self isScrollViewPanGestureRecognizer:otherGestureRecognizer]) {
+      RNSScreenView *topScreen = _reactSubviews.lastObject;
+      float end = [topScreen.gestureResponseDistance[@"end"] floatValue];
+      if (end >= 0) {
+        return NO;
+      }
+
       // ScrollView should take precedence when scrolling horizontally (it should be required to fail).
       // However, if it does not allow for horizontal scrolling, there should be no such restriction,
       // and swiping horizontally should dismiss the screen.
@@ -1224,8 +1230,11 @@ - (BOOL)gestureRecognizer:(UIGestureRecognizer *)gestureRecognizer
     // as `RNSScreenEdgeGestureRecognizer` but we don't want ScrollView being recognized on condition that the former
     // fails, but rather the opposite & that the ScrollView recognizer has a higher priority See also
     // gestureRecognizer:shouldRequireFailureOfGestureRecognizer
-    isEdgeSwipeGestureRecognizer =
-        isEdgeSwipeGestureRecognizer && gestureRecognizer != _controller.interactiveContentPopGestureRecognizer;
+    if (gestureRecognizer == _controller.interactiveContentPopGestureRecognizer) {
+      RNSScreenView *topScreen = _reactSubviews.lastObject;
+      float end = [topScreen.gestureResponseDistance[@"end"] floatValue];
+      isEdgeSwipeGestureRecognizer = end >= 0;
+    }
   }
 #endif // check for iOS >= 26
 
diff --git a/ios/tabs/host/RNSTabBarController.mm b/ios/tabs/host/RNSTabBarController.mm
index 289c9d958eb913a451161da11bda7fd44aeca377..0b39102d83a6aa4d61a589f0bc2fa425be9b6c62 100644
--- a/ios/tabs/host/RNSTabBarController.mm
+++ b/ios/tabs/host/RNSTabBarController.mm
@@ -300,6 +300,15 @@ - (BOOL)updateSelectedViewControllerTo:(nullable UIViewController *)nextSelected
     return YES;
   }
 
+  if (@available(iOS 27.0, *)) {
+    for (UITab *tab in self.tabs) {
+      if (tab.viewController == nextSelectedViewController) {
+        self.selectedTab = tab;
+        return YES;
+      }
+    }
+  }
+
   [self setSelectedViewController:nextSelectedViewController];
   return YES;
 }
@@ -382,6 +391,41 @@ - (BOOL)shouldPreventNativeTabSelection:(nonnull UIViewController *)nextViewCont
 // These methods are not called on programatic selection!
 // They are called only when a user taps on tab bar.
 
+- (BOOL)tabBarController:(UITabBarController *)tabBarController shouldSelectTab:(UITab *)tab API_AVAILABLE(ios(18.0))
+{
+  RCTAssert(tabBarController == self, @"[RNScreens] Unexpected type of controller: %@", tabBarController.class);
+
+  UIViewController *viewController = tab.viewController;
+  if (viewController == nil) {
+    return NO;
+  }
+
+  BOOL repeatedSelection = self.selectedTab == tab;
+  if (repeatedSelection) {
+    [self userDidRepeatViewControllerSelection:viewController];
+    return NO;
+  }
+
+  if ([self shouldPreventNativeTabSelection:viewController]) {
+    [self onDidPreventUserFromSelectingViewControllerWithKey:[self screenKeyForViewController:viewController]];
+    return NO;
+  }
+
+  _isHandlingExplicitSelectionUpdate = YES;
+  return YES;
+}
+
+- (void)tabBarController:(UITabBarController *)tabBarController
+            didSelectTab:(UITab *)selectedTab
+             previousTab:(nullable UITab *)previousTab API_AVAILABLE(ios(18.0))
+{
+  RCTAssert(tabBarController == self, @"[RNScreens] Unexpected type of controller: %@", tabBarController.class);
+  if (selectedTab.viewController != nil) {
+    [self userDidSelectViewController:selectedTab.viewController];
+  }
+  _isHandlingExplicitSelectionUpdate = NO;
+}
+
 - (BOOL)tabBarController:(UITabBarController *)tabBarController
     shouldSelectViewController:(UIViewController *)viewController
 {
@@ -488,7 +532,39 @@ - (void)updateReactChildrenControllers
     return;
   }
 
-  [self setViewControllers:_tabScreenControllers animated:[[self viewControllers] count] != 0];
+  if (@available(iOS 27.0, *)) {
+    NSMutableArray<UITab *> *tabs = [NSMutableArray arrayWithCapacity:_tabScreenControllers.count];
+
+    for (RNSTabsScreenViewController *viewController in _tabScreenControllers) {
+      RNSTabsScreenComponentView *screenView = viewController.tabScreenComponentView;
+      NSString *identifier = screenView.screenKey ?: [[NSUUID UUID] UUIDString];
+      UITab *tab;
+
+      if (screenView.systemItem == RNSTabsScreenSystemItemSearch) {
+        UISearchTab *searchTab = [[UISearchTab alloc]
+            initWithViewControllerProvider:^UIViewController *_Nonnull(__kindof UITab *_Nonnull providedTab) {
+              return viewController;
+            }];
+        searchTab.automaticallyActivatesSearch = YES;
+        tab = searchTab;
+      } else {
+        UITabBarItem *item = viewController.tabBarItem;
+        tab = [[UITab alloc] initWithTitle:item.title ?: @""
+                                    image:item.image
+                               identifier:identifier
+                   viewControllerProvider:^UIViewController *_Nonnull(__kindof UITab *_Nonnull providedTab) {
+                     return viewController;
+                   }];
+      }
+
+      tab.badgeValue = viewController.tabBarItem.badgeValue;
+      [tabs addObject:tab];
+    }
+
+    self.tabs = tabs;
+  } else {
+    [self setViewControllers:_tabScreenControllers animated:[[self viewControllers] count] != 0];
+  }
 }
 
 - (void)updateSelectedViewControllerIfNeeded
@@ -500,7 +576,7 @@ - (void)updateSelectedViewControllerIfNeeded
 
 - (void)updateSelectedViewController
 {
-  if (_pendingStateUpdate == nil || self.viewControllers.count == 0) {
+  if (_pendingStateUpdate == nil || _tabScreenControllers.count == 0) {
     return;
   }
 
@@ -601,7 +677,7 @@ - (void)updateTabBarAppearance
 
 - (void)updateTabBarA11yIfNeeded
 {
-  for (UIViewController *tabViewController in self.viewControllers) {
+  for (UIViewController *tabViewController in _tabScreenControllers) {
     auto screenView = static_cast<RNSTabsScreenViewController *>(tabViewController).tabScreenComponentView;
     if (!screenView.tabBarItemNeedsA11yUpdate) {
       continue;
@@ -620,7 +696,7 @@ - (nullable RNSTabsScreenViewController *)findChildViewControllerForKey:(nullabl
   if (screenKey == nil) {
     return nil;
   }
-  for (UIViewController *viewController in self.viewControllers) {
+  for (UIViewController *viewController in _tabScreenControllers) {
     RCTAssert([viewController isKindOfClass:RNSTabsScreenViewController.class],
               @"[RNScreens] Unexpected type of controller: %@",
               viewController.class);
