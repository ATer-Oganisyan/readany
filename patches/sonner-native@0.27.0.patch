diff --git a/lib/commonjs/gestures.js b/lib/commonjs/gestures.js
index 581796369046a65673c5707ccad81eec8561ecf1..d0006ad86a8c5f287688026e4e37f481f6a98af3 100644
--- a/lib/commonjs/gestures.js
+++ b/lib/commonjs/gestures.js
@@ -48,7 +48,7 @@ const ToastSwipeHandler = ({
     if (!enabled) {
       return;
     }
-    if (direction === 'left' && event.translationX < 0) {
+    if (direction === 'horizontal') {
       translate.value = event.translationX;
     } else if (direction === 'up') {
       const isBottomPosition = position === 'bottom-center';
@@ -71,9 +71,9 @@ const ToastSwipeHandler = ({
     if (!enabled) {
       return;
     }
-    if (direction === 'left') {
-      const threshold = -WINDOW_WIDTH * 0.25;
-      const shouldDismiss = translate.value < threshold;
+    if (direction === 'horizontal') {
+      const threshold = WINDOW_WIDTH * 0.25;
+      const shouldDismiss = Math.abs(translate.value) > threshold;
       if (Math.abs(translate.value) < 16) {
         translate.value = (0, _reactNativeReanimated.withTiming)(0, {
           easing: _reactNativeReanimated.Easing.elastic(0.8)
@@ -81,7 +81,8 @@ const ToastSwipeHandler = ({
         return;
       }
       if (shouldDismiss) {
-        translate.value = (0, _reactNativeReanimated.withTiming)(-WINDOW_WIDTH, {
+        const exitX = translate.value < 0 ? -WINDOW_WIDTH : WINDOW_WIDTH;
+        translate.value = (0, _reactNativeReanimated.withTiming)(exitX, {
           easing: _reactNativeReanimated.Easing.inOut(_reactNativeReanimated.Easing.ease)
         }, isDone => {
           if (isDone) {
@@ -134,7 +135,7 @@ const ToastSwipeHandler = ({
   const isAndroid = _reactNative.Platform.OS === 'android';
   const animatedStyle = (0, _reactNativeReanimated.useAnimatedStyle)(() => {
     const aStyle = {
-      transform: [direction === 'left' ? {
+      transform: [direction === 'horizontal' ? {
         translateX: translate.value
       } : {
         translateY: translate.value * (position === 'bottom-center' ? -1 : 1)
@@ -143,7 +144,9 @@ const ToastSwipeHandler = ({
     if (isAndroid) {
       aStyle.opacity = 1;
     } else {
-      aStyle.opacity = (0, _reactNativeReanimated.interpolate)(translate.value, [0, direction === 'left' ? -WINDOW_WIDTH : -60], [1, 0]);
+      aStyle.opacity = direction === 'horizontal'
+        ? (0, _reactNativeReanimated.interpolate)(Math.abs(translate.value), [0, WINDOW_WIDTH], [1, 0])
+        : (0, _reactNativeReanimated.interpolate)(translate.value, [0, -60], [1, 0]);
     }
     return aStyle;
   }, [direction, translate]);
diff --git a/lib/module/gestures.js b/lib/module/gestures.js
index 006393c2afe8dfe45719948c6df344a925e0c717..5e24a31033b6cb89aacfeddde2dc1eb4fd58c8fd 100644
--- a/lib/module/gestures.js
+++ b/lib/module/gestures.js
@@ -41,7 +41,7 @@ export const ToastSwipeHandler = ({
     if (!enabled) {
       return;
     }
-    if (direction === 'left' && event.translationX < 0) {
+    if (direction === 'horizontal') {
       translate.value = event.translationX;
     } else if (direction === 'up') {
       const isBottomPosition = position === 'bottom-center';
@@ -64,9 +64,9 @@ export const ToastSwipeHandler = ({
     if (!enabled) {
       return;
     }
-    if (direction === 'left') {
-      const threshold = -windowWidth * 0.25;
-      const shouldDismiss = translate.value < threshold;
+    if (direction === 'horizontal') {
+      const threshold = windowWidth * 0.25;
+      const shouldDismiss = Math.abs(translate.value) > threshold;
       if (Math.abs(translate.value) < 16) {
         translate.value = withTiming(0, {
           easing: Easing.elastic(0.8)
@@ -74,7 +74,8 @@ export const ToastSwipeHandler = ({
         return;
       }
       if (shouldDismiss) {
-        translate.value = withTiming(-windowWidth, {
+        const exitX = translate.value < 0 ? -windowWidth : windowWidth;
+        translate.value = withTiming(exitX, {
           easing: Easing.inOut(Easing.ease)
         }, isDone => {
           if (isDone) {
@@ -130,7 +131,7 @@ export const ToastSwipeHandler = ({
   const isAndroid = Platform.OS === 'android';
   const animatedStyle = useAnimatedStyle(() => {
     const aStyle = {
-      transform: [direction === 'left' ? {
+      transform: [direction === 'horizontal' ? {
         translateX: translate.value
       } : {
         translateY: translate.value * (position === 'bottom-center' ? -1 : 1)
@@ -139,7 +140,9 @@ export const ToastSwipeHandler = ({
     if (isAndroid) {
       aStyle.opacity = 1;
     } else {
-      aStyle.opacity = interpolate(translate.value, [0, direction === 'left' ? -windowWidth : -60], [1, 0]);
+      aStyle.opacity = direction === 'horizontal'
+        ? interpolate(Math.abs(translate.value), [0, windowWidth], [1, 0])
+        : interpolate(translate.value, [0, -60], [1, 0]);
     }
     return aStyle;
   }, [direction, translate, windowWidth]);
diff --git a/lib/typescript/src/types.d.ts b/lib/typescript/src/types.d.ts
index 88183d28f539d3bd5fd1645b001ede6be37f35b4..7dea78de60d3410b32bbf83090be4bd125232078 100644
--- a/lib/typescript/src/types.d.ts
+++ b/lib/typescript/src/types.d.ts
@@ -31,7 +31,7 @@ type PromiseOptions<T = unknown> = {
 };
 export type ToastPosition = 'top-center' | 'bottom-center' | 'center';
 export type ToastTheme = 'light' | 'dark' | 'system';
-export type ToastSwipeDirection = 'left' | 'up';
+export type ToastSwipeDirection = 'left' | 'horizontal' | 'up';
 export type ToastVariant = 'success' | 'error' | 'warning' | 'info' | 'loading';
 export type ToastEntryExitAnimation = EntryExitAnimationFunction | BaseAnimationBuilder | typeof BaseAnimationBuilder | Keyframe | 'default';
 export type ToastAnimation = {
diff --git a/src/gestures.tsx b/src/gestures.tsx
index a27988c3740c6a15165dfc2aa72d66b7fada1899..26cfb8a40e7c1070c6ea8df95e53a38c39d6d73f 100644
--- a/src/gestures.tsx
+++ b/src/gestures.tsx
@@ -67,7 +67,7 @@ export const ToastSwipeHandler: React.FC<
         return;
       }
 
-      if (direction === 'left' && event.translationX < 0) {
+      if (direction === 'horizontal') {
         translate.value = event.translationX;
       } else if (direction === 'up') {
         const isBottomPosition = position === 'bottom-center';
@@ -92,9 +92,9 @@ export const ToastSwipeHandler: React.FC<
         return;
       }
 
-      if (direction === 'left') {
-        const threshold = -windowWidth * 0.25;
-        const shouldDismiss = translate.value < threshold;
+      if (direction === 'horizontal') {
+        const threshold = windowWidth * 0.25;
+        const shouldDismiss = Math.abs(translate.value) > threshold;
 
         if (Math.abs(translate.value) < 16) {
           translate.value = withTiming(0, {
@@ -104,8 +104,9 @@ export const ToastSwipeHandler: React.FC<
         }
 
         if (shouldDismiss) {
+          const exitX = translate.value < 0 ? -windowWidth : windowWidth;
           translate.value = withTiming(
-            -windowWidth,
+            exitX,
             {
               easing: Easing.inOut(Easing.ease),
             },
@@ -168,7 +169,7 @@ export const ToastSwipeHandler: React.FC<
   const animatedStyle = useAnimatedStyle(() => {
     const aStyle: ViewStyle = {
       transform: [
-        direction === 'left'
+        direction === 'horizontal'
           ? { translateX: translate.value }
           : {
               translateY:
@@ -180,11 +181,10 @@ export const ToastSwipeHandler: React.FC<
     if (isAndroid) {
       aStyle.opacity = 1;
     } else {
-      aStyle.opacity = interpolate(
-        translate.value,
-        [0, direction === 'left' ? -windowWidth : -60],
-        [1, 0]
-      );
+      aStyle.opacity =
+        direction === 'horizontal'
+          ? interpolate(Math.abs(translate.value), [0, windowWidth], [1, 0])
+          : interpolate(translate.value, [0, -60], [1, 0]);
     }
 
     return aStyle;
diff --git a/src/types.ts b/src/types.ts
index 62eb35eee90fee5a3181b4499f2a709fe41c1984..8d14fd43606ed336a4a8bf5c64e27712ac7db2f2 100644
--- a/src/types.ts
+++ b/src/types.ts
@@ -41,7 +41,7 @@ export type ToastPosition = 'top-center' | 'bottom-center' | 'center';
 
 export type ToastTheme = 'light' | 'dark' | 'system';
 
-export type ToastSwipeDirection = 'left' | 'up';
+export type ToastSwipeDirection = 'left' | 'horizontal' | 'up';
 
 export type ToastVariant = 'success' | 'error' | 'warning' | 'info' | 'loading';
 
